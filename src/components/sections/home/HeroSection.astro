---
import Dots from '@components/atoms/Dots.astro';
import { Button } from '@components/atoms/NewButton';
import Pattern from '@components/atoms/Pattern.astro';
import Subtitle from '@components/atoms/Subtitle.astro';
import WavesCircle from '@components/atoms/WavesCircle.astro';
import HeroImage from '@images/Lusteniny.jpg';
import { Image } from 'astro:assets';
import tailwindColors from 'tailwindcss/colors';
---

<section class="container relative mx-auto px-4 pb-32 pt-12">
  <Pattern
    class="absolute left-0 top-[50%] -translate-y-1/2"
    backgroundColor={tailwindColors.slate[100]}
    lineColor={tailwindColors.slate[300]}
    spotColor={tailwindColors.slate[100]}
    width={1440}
    height={992}
    id="hero"
  />
  <div class="relative grid grid-cols-2 items-center gap-8">
    <div class="">
      <Subtitle variant="light" text="Luštěniny" />
      <h1
        class="mb-6 text-3xl font-bold leading-[1.1] tracking-wide text-slate-900 md:text-5xl md:leading-[1.1] lg:text-6xl lg:leading-[1.1]"
      >
        Šifrovací hra <br /> v centru Zlína
      </h1>
      <p
        class="mb-8 max-w-2xl text-lg font-medium leading-normal tracking-wide text-slate-600 md:text-xl md:leading-normal lg:text-2xl lg:leading-normal"
      >
        Luštěniny jsou tradiční šifrovací hra pořádána v centru Zlína, které se můžeš zúčastnit jak v týmu, tak sám.
        Neváhej a přijd si k nám zaluštit.
      </p>
      <div class="flex flex-wrap">
        <Button to="/#informace" text="Více informací" size="large" icon="arrow-right" />
      </div>
    </div>
    <div class="relative mx-auto max-w-max pl-12 md:mr-0">
      <WavesCircle class="absolute -left-8 -top-12 z-10 h-32 w-32 text-green-500 md:w-auto" />
      <Dots class="absolute -bottom-8 -right-7 z-10 w-28 text-green-500 md:w-auto" />
      <Image src={HeroImage} alt="" class="relative aspect-[3/2] rounded-[1.5rem] object-cover shadow-xl" />
    </div>
  </div>
</section>
